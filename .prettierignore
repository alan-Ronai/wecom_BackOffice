legacy
docs
design
pnpm-lock.yaml
*.md
# ported verbatim from legacy/css/app.css — keep byte-identical for visual diffing
apps/web/src/styles/app.css
# generated by `msw init`; must stay byte-identical to the installed msw version
apps/web/public/mockServiceWorker.js
# hand-formatted compose files owned by L1 — prettier only wants to requote them,
# and rewriting them here would collide with that lane
deploy/docker-compose.yml
deploy/docker-compose.ci.yml
