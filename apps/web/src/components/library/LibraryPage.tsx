/** Placeholder — implemented in a later task of the L4 plan. */
export function LibraryPage(_props: { mode: 'library' | 'pinned' | 'recent' | 'drafts' }) {
  return <div className="route-loading">בקרוב</div>;
}
