import type { RequestHandler } from 'msw';

/** Filled in by the fixtures task; kept here so the setup file has a stable import. */
export const handlers: RequestHandler[] = [];
export const resetState = (): void => {};
