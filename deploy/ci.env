NODE_ENV=production
PORT=3000
DATABASE_URL=postgres://kb:ci@db:5432/kb
SESSION_SECRET=ci-secret-ci-secret-ci-secret-1234
PUBLIC_URL=https://localhost:8443
OIDC_ISSUER=
OIDC_CLIENT_ID=
OIDC_CLIENT_SECRET=
OIDC_REDIRECT_URI=
AUTH_FALLBACK=none
PALOALTO_HOST=
PALOALTO_API_KEY=
PALOALTO_SUBNETS=
MODEL_URL=http://ollama:11434
MODEL_NAME=qwen2.5:3b-instruct-q4_K_M
# Not a real secret — CI-only, non-default so ConfigSchema's production guard passes.
CONNECTOR_KEY=85ae2b16a6a0e4c65705baf6806f86e1e5a935a16ff127c337053b84f0256825
# Both are required when NODE_ENV=production (see ConfigSchema). `*` here because the CI
# stack talks to nothing outbound; a real install names its connector hosts.
CONNECTOR_HOST_ALLOWLIST=*
TRUST_PROXY=true
TRASH_DAYS=30
BACKUP_DIR=/backups
MIGRATE_ON_START=true
POSTGRES_PASSWORD=ci
TLS_CERT_PATH=./certs/cert.pem
TLS_KEY_PATH=./certs/key.pem
BACKUP_RETENTION_DAYS=14
WEB_HTTPS_PORT=8443
WEB_HTTP_PORT=8080
